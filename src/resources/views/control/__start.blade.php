<div 
    class="{{ $class }}" 
    id="{{$id}}" 
    data-name="{{$name}}" 
    data-readonly="{{$readonly}}"
>
        
    <div class="card {{ $color?'bg-'.$color. ' with-bg':'' }} ">
        

        